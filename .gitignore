# environment
.env
.env.*
!.env.example

# python
__pycache__/
*.py[cod]
*.pyo
*.pyd
venv/
.venv/
*.egg-info/
dist/
build/
.eggs/

# data & db — full db stays local, slim db is committed for deployment
*.duckdb
*.duckdb.wal
!gridpulse_slim.duckdb

# model artifacts — commit lgb/pkl for deployment, exclude stale keras files
models/artifacts/*.keras

# rag corpus and chroma index — committed for deployment (small, pre-built)

# testing & linting
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/

# secrets scanning
.secrets.baseline

# ide
.vscode/
.idea/

# os
.DS_Store
Thumbs.db

# docker
.dockerignore

# terraform
infra/.terraform/
infra/*.tfstate
infra/*.tfstate.backup
infra/*.tfvars


4 hour progress.md